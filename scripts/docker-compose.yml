version: '3'
services:
  nginx:
    image: nginx:latest
    restart: always
    ports:
      - 80:80
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    links:
      - web
      - tr069
    networks:
      - mynetwork
  web:
    image: web
    restart: always
    ports:
      -   8081:8081
    networks:
      - mynetwork
  tr069:
    image: tr069
    restart: always
    networks:
      - mynetwork
  db:
    build: mysql
    restart: always
    volumes:
      - mysql-db:/var/lib/mysql
    networks:
      mynetwork:
        aliases:
          - db.main
          - db.syslog
volumes:
  mysql-db:

networks:
  mynetwork:
    driver: bridge